// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("say", "for decrypt support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("decrypt", "enable decrypt support", "no");

if (PHP_DECRYPT != "no") {
	EXTENSION("decrypt", "decrypt.c");
}

